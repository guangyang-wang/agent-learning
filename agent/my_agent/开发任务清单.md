# 开发任务清单（可勾选）

> 来源：`README.md` 的「8 阶段落地路线图」+ `架构设计与业务场景.md` 的细节。
>
> **怎么打勾**：VS Code / GitHub / Typora 里直接点击复选框；纯文本里把 `[ ]` 改成 `[x]`。
> 建议按 0 → 8 顺序推进，每阶段**可运行、可演示**，最后拼成完整系统。

---

## 阶段 0：环境准备

- [x] 创建虚拟环境：`python -m venv .venv` 并激活（Windows：`.venv\Scripts\activate`）
- [x] 安装依赖：`pip install -r requirements.txt`
- [x] 可编辑安装：`pip install -e .`（让 `agent` 包可 import）
- [x] 配置 `.env`：复制 `.env.example`，填入 `DEEPSEEK_API_KEY`

---

## 阶段 1：单 Agent ReAct + 2~3 工具（最小闭环 demo）

- [x] `llm/factory.py`：实现可插拔 LLM 工厂（默认 DeepSeek）
- [X] `state.py`：定义 `AgentState` 最小字段（`user_input` / `messages` / `iteration_count`）
- [x] `tools/registry.py` + `tools/safe_tools.py`：封装 2~3 个安全工具（如公式计算、查询）
- [x] `graph/react_graph.py`：`agent` 节点 ↔ `tools` 节点 的 ReAct 循环 + `conditional_edges`
- [x] `main.py`：跑通一个简单问答
- [x] 验证：单题问答 / 多工具连续调用能跑通

---

## 阶段 2：RAG + Checkpointer 记忆（能查课件、记得上下文）

- [ ] `rag/loader.py`：PDF 课件解析（pypdf）
- [ ] `rag/splitter.py`：文档分块（chunking + overlap）
- [ ] `rag/embedding.py`：向量化
- [ ] `rag/vector_store.py`：Chroma 向量库存取
- [ ] `rag/retriever.py`：检索 + 空结果回退（标注「无课件依据」）
- [ ] `memory/short_term.py`：Checkpointer 持久化短期记忆
- [ ] `memory/long_term.py`：向量库存经验摘要（可后置）
- [ ] 验证：上传课件后能检索到正确片段并回答

---

## 阶段 3：Plan-and-Execute 子图（第一个子图）

- [ ] `graph/plan_graph.py`：planner 节点生成任务计划（结构校验步骤数 / 依赖）
- [ ] 按依赖序（拓扑序）分发子任务
- [ ] 子 Agent 内部复用 ReAct 循环
- [ ] 子任务失败 → 局部重规划（保留已成功的子任务）
- [ ] 验证：复杂多步任务（如写代码 + 报告）能按计划执行

---

## 阶段 4：多 Agent 辩论子图（第二个子图）

- [ ] `graph/debate_graph.py`：立论 / 批判 / 仲裁 三个对等 Agent
- [ ] State 中 `debate_messages` 消息池（三 Agent 轮流 append）
- [ ] 收敛判断：观点重复 / 最大轮次上限
- [ ] 验证：论文评审能输出完整评审报告

---

## 阶段 5：路由分类器，三图合一（核心创新点）

- [ ] `graph/router.py`：任务复杂度分类器（LLM + 规则双保险）
- [ ] `main.py`：三图合一，`add_conditional_edges` 按 `task_type` 分流
- [ ] 状态复用：Checkpointer 跨图共享 `messages`，支持动态切换
- [ ] 验证：simple → complex 升级能自动切换子图

---

## 阶段 6：防死循环 + 人机确认（安全中间层）

- [ ] `safety/anti_loop.py`：四层机制（轮次上限 / 参数去重 / 信息增益 / 失败重试）
- [ ] `safety/hitl.py`：`interrupt()` 挂起 + `Command(resume=...)` 恢复
- [ ] `tools/dangerous_tools.py` + `registry.py`：工具安全分级（安全自动放行 / 高危需确认）
- [ ] 验证：死循环被拦截；高危工具（代码运行 / 写文件）需用户确认

---

## 阶段 7：MCP + Skill 插件化（能力扩展）

- [ ] `skills/base.py`：Skill 基类
- [ ] 各 Skill：`course_qa` / `code_experiment` / `report_gen` / `paper_review`
- [ ] `mcp/client.py`：MCP 协议统一访问向量库 / 文件 / 沙箱
- [ ] 验证：Agent 按任务类型动态加载对应 Skill

---

## 阶段 8：评估 + FastAPI 部署 + 追踪（从「能跑」到「能答辩」）

- [ ] `evaluation/metrics.py` + `benchmark.py`：五指标（分类准确率 / 正确率 / 轮数 / 成本 / 延迟）
- [ ] `api/app.py` + `api/schemas.py`：FastAPI 接口
- [ ] `observability/tracing.py`：LangSmith 全链路追踪
- [ ] 验证：能说出「准确率 X% / 单次成本 Y 元 / P99 延迟 Z 秒」

---

## 附：答辩两大亮点（对应阶段 6、阶段 5/场景四）

- [ ] 亮点 1：分层范式（1 底座 ReAct + 2 编排 Plan/辩论）——贯穿阶段 1~5
- [ ] 亮点 2：人机在环安全确认（`interrupt()`）——阶段 6
- [ ] 创新点：动态模式切换（一套系统三架构自适应）——阶段 5 / 场景四